# URL backend API
# Untuk development lokal (saat backend dijalankan terpisah):
VITE_API_URL=http://localhost:3001/api

# Untuk production di Vercel (tidak perlu diset, default ke /api):
# VITE_API_URL=/api
